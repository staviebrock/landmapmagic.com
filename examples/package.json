{
  "name": "@landmapmagic/examples",
  "private": true,
  "version": "0.1.0",
  "description": "Examples for @landmapmagic package",
  "scripts": {
    "dev": "npm run build:parent && vite",
    "dev:fresh": "npm run build:parent && npm install && vite",
    "build:parent": "cd .. && npm run build",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "landmapmagic": "file:..",
    "mapbox-gl": "^2.15.0",
    "maplibre-gl": "^3.6.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@vitejs/plugin-react": "^4.2.0",
    "dotenv": "^17.2.2",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
